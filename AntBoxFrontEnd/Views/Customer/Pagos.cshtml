@model AntBoxFrontEnd.Models.PagosModel
@{
    ViewBag.Title = "Index";
}
<!-- WRAPPER -->
<div class="wrapper">
    <!-- CONTENT -->
    <div class="content pagos">
        <!-- CONTAINER -->
        <div class="container">
            <div class="row irow-xs">
                <div class="col-sm-12">
                    <h1>Pagos</h1>
                    <div class="heading-divider"></div>
                </div>
            </div>
        </div>
        <!-- /.container -->
        <!-- CONTAINER -->
        <div class="container mt30">
            <div class="row">
                <div class="col-md-3 pr0 no_portrait">
                    @Html.Partial("_LeftMenu")
                </div>
                <div class="col-md-9">
                    <div class="row pl30 pr30 recibos">
                        <div class="irow-xs pb30">
                            <h3>Recibos y facturación</h3>
                            <p>Revisa tus recibos de pago y emite tus facturas del mes en curso. Recuerda que antes de facturar debes tener registrada tus datos fiscales en la parte inferior de esta página.</p>
                        </div>
                        @if (Model.Payments != null)
                        {
                            foreach (var item in Model.Payments)
                            {
                                <div class="col-sm-12 back_gris pt15 pb20 mb10">
                                    <div class="media-left">
                                        <div class="icon icon-color icon-size-4">
                                            <i class="fa fa-file-text-o"></i>
                                        </div>
                                    </div>
                                    <div class="media-body w100 pt10">
                                        @if (item.Status == "completed")
                                        {
                                            <h4>Pagado</h4>
                                        }
                                        @if (item.Billed == true)
                                        {
                                            <div class="sticker_pay completed">Facturado</div>
                                        }
                                        <p>@item.Fecha</p>
                                        <button type="button" class="btn btn-primary btn-sm pull-right pb10 ml10" onclick="verRecibo()">Ver Recibo</button>
                                        <button type="button" class="btn btn-primary btn-sm btn_blue pull-right pb10 ml10 facturar_button" data-id="@item.Id" data-billed="@item.Billed">Facturar</button>
                                    </div>
                                </div>
                            }
                        }
                        @*<div class="col-sm-12 back_gris pt15 pb20 mb10">
                            <div class="media-left">
                                <div class="icon icon-color icon-size-4">
                                    <i class="fa fa-file-text-o"></i>
                                </div>
                            </div>
                            <div class="media-body w100 pt10">
                                <h4>Pagado</h4>
                                <div class="sticker_pay in_progress">Recibo Vencido</div>
                                <p>15/Marzo/2016</p>
                                <button type="button" class="btn btn-primary btn-sm pull-right pb10 ml10" onclick="verRecibo()">Ver Recibo</button>
                            </div>
                        </div>
                        <div class="col-sm-12 back_gris pt15 pb20 mb10">
                            <div class="media-left">
                                <div class="icon icon-color icon-size-4">
                                    <i class="fa fa-file-text-o"></i>
                                </div>
                            </div>
                            <div class="media-body w100 pt10">
                                <h4>Pagado</h4>
                                <div class="sticker_pay completed">Facturado</div>
                                <p>22/Julio/2016</p>
                                <button type="button" class="btn btn-primary btn-sm pull-right pb10 ml10" onclick="verRecibo()">Ver Recibo</button>
                                <button type="button" class="btn btn-primary btn-sm btn_blue pull-right pb10 ml10" data-toggle="modal" data-target="#enviar_factura">Enviar Facturar</button>
                            </div>
                        </div>
                        <div class="col-sm-12 back_gris pt15 pb20 mb10">
                            <div class="media-left">
                                <div class="icon icon-color icon-size-4">
                                    <i class="fa fa-file-text-o"></i>
                                </div>
                            </div>
                            <div class="media-body w100 pt10">
                                <h4>Pagado</h4>
                                <p>01/Agosto/2016</p>
                                <button type="button" class="btn btn-primary btn-sm pull-right pb10 ml10" onclick="verRecibo()">Ver Recibo</button>
                                <button type="button" class="btn btn-primary btn-sm btn_blue pull-right pb10 ml10" data-toggle="modal" data-target="#facturar">Facturar</button>
                            </div>
                        </div>*@
                    </div>
                    <div class"row">
                        <div class="col-sm-12 pl0 pr0 mt20 text-center">
                            <!-- PAGINATION -->
                            <ul class="pagination solid">
                                <li class="pull-left"><a href="#" class="prev"><i class="fa fa-angle-left"></i></a></li>
                                <li class="pull-right"><a href="#" class="next"><i class="fa fa-angle-right"></i></a></li>
                                <li class="active"><a href="#">1</a></li>
                                @*<li><a href="#">2</a></li>
                                <li><a href="#">3</a></li>
                                <li><a href="#">4</a></li>
                                <li><a href="#">…</a></li>
                                <li><a href="#">36</a></li>*@
                            </ul>
                            <!-- /.pagination -->
                        </div>
                    </div>
                    <div class="row mt20">
                        <div class="col-sm-12">
                            <hr class="hr-dashed mt40 mb20" />
                        </div>
                    </div>

                    <div class="row mt20 pl30 pr30 tarjetas">
                        <div class="irow-xs pb30">
                            <h3>Tarjetas registradas</h3>
                            <p>Recuerda que tendrás que tener una tarjeta registrada como mínimo para realizar tu primer orden. Tu primer tarjeta se asignará de maner automática para los pagos recurrentes.</p>
                        </div>
                        <div id="editCardResult"></div>
                        <div id="CardList">
                            @Html.Partial("_CustomerCards", Model.Cards)
                        </div>
                    </div>
                    
                    <div class="row mt20">
                        <div class="col-sm-12">
                            <hr class="hr-dashed mt40 mb30" />
                        </div>
                    </div>
                    <div class="row mt10 pl30 pr30">
                        <div class="irow-xs">
                            <h3>Registrar una nueva tarjeta</h3>
                            <p>Puedes registrar tantas tarjetas de crédito como tú decidas.</p>
                        </div>
                        <div class="col-sm-12 pl0 pr0">
                            <form action="" method="post" class="form-solid form-card" novalidate>
                                <div class="row mt20">
                                    <div class="col-sm-6">
                                        <div class="form-group">
                                            <label for="send-form-name">*Nombre de Tarjeta</label>
                                            <input type="text" name="name" id="card-holder-name">
                                        </div>
                                    </div>
                                    <div class="col-sm-6">
                                        <div class="form-group">
                                            <label for="send-form-email">*Número de Tarjeta</label>
                                            <input type="text" name="numero" id="card-number">
                                        </div>
                                    </div>
                                </div>
                                <p class="mb0 etiqueta">Expiración:</p>
                                <div class="row">
                                    <div class="col-sm-4">
                                        <div class="form-group">
                                            <label for="send-form-email">*MM</label>
                                            <input type="text" name="mm" id="card-exp-month">
                                        </div>
                                    </div>
                                    <div class="col-sm-4">
                                        <div class="form-group">
                                            <label for="send-form-email">*YY</label>
                                            <input type="text" name="yy" id="card-exp-year">
                                        </div>
                                    </div>
                                    <div class="col-sm-4">
                                        <div class="form-group">
                                            <label for="send-form-email">*CVC</label>
                                            <input type="text" name="cvv" id="card-cvv">
                                        </div>
                                    </div>
                                </div>

                                <input type="button" class="btn btn-primary btn-wide pull-right mt10" value="Registrar" onclick="generate_token()">
                                <p class="succs-msg">Мensaje enviado</p>
                            </form>
                            <div id="newCardResult"></div>
                        </div>
                    </div>
                    <div class="row mt20">
                        <div class="col-sm-12">
                            <hr class="hr-dashed mt40 mb30" />
                        </div>
                    </div>
                    <div class="row pl30 pr30 mt10">
                        <div class="irow-xs pb30">
                            <h3>Datos de Facturación</h3>
                            <p>Registra tus datos fiscales de facturación para tus recibos.</p>
                            <div class="alert alert-warning alert-dismissible fade in">
                                <button type="button" class="close" data-dismiss="alert">
                                    <i class="fa fa-times"></i>
                                </button>
                                <p><i class="fa fa-exclamation-triangle"></i> <strong>Regístra una dirección como mínimo.</strong><br />Recuerda que para realizar cualquier factura deberás tener por lo menos un dato de facturación.</p>
                            </div>
                        </div>
                        <div class="content-datos-facturacion">
                            @if (Model.Address != null)
                            {
                                foreach (var item in Model.Address)
                                {
                                    <div class="col-sm-12 back_gris pt30 pb20 mb30 div-facturacion">
                                        <div class="media-left">
                                            <div class="icon icon-color icon-size-4">
                                                <i class="fa fa-file-text"></i>
                                            </div>
                                        </div>
                                        <div class="media-body" data-id="@item.Id">
                                            <h4 class="identificaciont">@item.Alias</h4>
                                            <p>Nombre o Razón Social: <span class="nombreempresat">@item.Bussiness_name</span><br>RFC: <span class="razonsocialt">@item.Rfc_id</span></p>
                                            <button type="button" class="btn btn-primary btn-sm pull-right ml10 open-editar-facturacion">Editar</button>
                                            <button type="button" class="btn btn-primary btn-sm pull-right btn_blue eliminar-facturacion">Eliminar</button>
                                        </div>
                                    </div>
                                }
                            }
                            
                        </div>
                        
                    </div>
                    
                    <div class="row mt20">
                        <div class="col-sm-12">
                            <hr id="registrar_nuevos_datos_facturacion" class="hr-dashed mt40 mb0" />
                        </div>
                    </div>
                    
                    <div class="row pl30 pr30">
                        <div id="loader1" role="alert" style="display: none;">
                            <img src="@Url.Content("~/Images/preloader.gif")" />   Agregando direccion de facturacion... &nbsp;
                        </div>
                        <div class="irow-xs pb30">
                            <h3>Regístra nuevos datos de facturación</h3>
                            <p>Registra tus datos de facturación para tus recibos. Asígnale un nombre para identificarla fácilmente más adelante.</p>
                        </div>
                        <form method="post" class="form-afacturacion form-solid" novalidate>
                            <div class="row">
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label for="aidentificacion">* Nombre de Identificación</label>
                                        <input type="text" name="Alias" id="aidentificacion">
                                    </div>
                                    <div class="form-group">
                                        <label for="arazonsocial">* Nombre o Razón Social</label>
                                        <input type="text" name="Bussiness_name" id="arazonsocial">
                                    </div>
                                    <div class="form-group">
                                        <label for="arfc">* RFC</label>
                                        <input type="text" name="Rfc_id" id="arfc">
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-email">* Calle</label>
                                        <input type="text" name="Street" id="acalle">
                                    </div>
                                    <div class="row">
                                        <div class="col-sm-6">
                                            <div class="form-group">
                                                <label for="send-form-email">* Núm. Ext.</label>
                                                <input type="text" name="External_number" id="aext">
                                            </div>
                                        </div>
                                        <div class="col-sm-6">
                                            <div class="form-group">
                                                <label for="send-form-email">Núm. Int.</label>
                                                <input type="text" name="Internal_number" id="aint">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-email">* Código Postal</label>
                                        <input type="text" name="Zipcode" id="azipcode">
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label for="form-select">* Delegación o Municipio</label>
                                        <input type="text" name="Delegation" id="amunicipio">
                                    </div>
                                    <div class="form-group">
                                        <label for="form-select">* Colonia</label>
                                        <input type="text" name="Neighborhood" id="acolonia">
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-email">* Ciudad</label>
                                        <input type="text" name="City" id="aciudad">
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-name">* Estado</label>
                                        <input type="text" name="State" id="aestado">
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-email">* País</label>
                                        <input type="text" name="Country" id="apais">
                                    </div>
                                    <div class="form-group">
                                        <label for="send-form-email">* E-mail</label>
                                        <input type="email" name="Billing_email" id="aemail">
                                    </div>
                                </div>
                                <div class="col-sm-12 mt10">
                                    <input type="submit" data-verif="1" class="btn btn-primary btn-wide pull-right guardar-facturacion" value="Guardar">
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <!-- /.container -->
    </div>
    <!-- /.content -->

</div>
<!-- /.wrapper -->

<!-- Modal Small -->
<div class="modal fade" id="facturar">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><i class="fa fa-times"></i></button>
                <h6 class="modal-title">Facturar</h6>
            </div>
            <div class="modal-body">
                <form method="post" class="contact-form form-solid form-facturar" novalidate>
                    <div id="loader-facturacion" role="alert" style="display: none;">
                        <img src="@Url.Content("~/Images/preloader.gif")" />   Procesando facturacion... &nbsp;
                    </div>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group mb0">
                                <input type="hidden" name="idpago" id="idpago" value="" />
                                <label for="form-select">Datos de facturación</label>
                                <select name="direccion">
                                    <option value="">- Selecciona una opción -</option>
                                    @if (Model.Address != null)
                                    {
                                        foreach (var item in Model.Address)
                                        {
                                            <option value="@item.Id">@item.Alias</option>
                                        }
                                    }
                                </select>
                                @*<a href="pagos.php#registrar_nuevos_datos_facturacion" onclick="goto_rndf();" class="pull-right">Nuevos datos de facturación</a>*@
                            </div>
                        </div>
                        <div class="col-sm-12 mt10">
                            <input type="submit" class="btn btn-primary btn-wide pull-right facturar-button-form" value="Facturar">
                            <p class="succs-msg">Мensaje enviado</p>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!-- /.modal -->
<!-- Modal Small -->
<div class="modal fade" id="enviar_factura">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><i class="fa fa-times"></i></button>
                <h6 class="modal-title">Enviar factura por email</h6>
            </div>
            <div class="modal-body">
                <form action="../../php/email.php" method="post" class="contact-form form-solid" novalidate>
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group">
                                <label for="send-form-name">Email</label>
                                <input type="email" name="name" id="send-form-name">
                            </div>
                            <div class="form-group">
                                <label for="send-form-message">Descripción</label>
                                <textarea name="message" id="send-form-message"></textarea>
                            </div>
                        </div>
                        <div class="col-sm-12 mt10">
                            <input type="submit" class="btn btn-primary btn-wide pull-right" value="Enviar">
                            <p class="succs-msg">Мensaje enviado</p>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!-- /.modal -->
<!-- Modal Medium -->
<div class="modal fade" id="editar_facturacion">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><i class="fa fa-times"></i></button>
                <h6 class="modal-title">Editar dirección de facturación</h6>
            </div>
            <div class="modal-body">
                <div id="loader2" role="alert" style="display: none;">
                    <img src="@Url.Content("~/Images/preloader.gif")" />   Guardando datos de facturacion... &nbsp;
                </div>
                <form method="post" class="form-efacturacion form-solid" novalidate>
                    <input type="hidden" name="id" id="idFacturacion" value="" />
                    <div class="row">
                        <div class="col-sm-6">
                            <div class="form-group">
                                <label for="identificacion">Nombre de Identificación*</label>
                                <input type="text" name="alias" id="eidentificacion">
                            </div>
                            <div class="form-group">
                                <label for="razonsocial">Nombre o Razón Social*</label>
                                <input type="text" name="razonsocial" id="erazonsocial">
                            </div>
                            <div class="form-group">
                                <label for="rfc">RFC*</label>
                                <input type="text" name="rfc" id="erfc">
                            </div>
                            <div class="form-group">
                                <label for="send-form-email">Calle*</label>
                                <input type="text" name="Street" id="ecalle">
                            </div>
                            <div class="row">
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label for="send-form-email">Núm. Ext.*</label>
                                        <input type="text" name="External_number" id="eext">
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <label for="send-form-email">Núm. Int.</label>
                                        <input type="text" name="Internal_number" id="eint">
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="send-form-email">Código Postal*</label>
                                <input type="text" name="Zipcode" id="ezipcode">
                            </div>
                        </div>
                        <div class="col-sm-6 pl0_modal">
                            <div class="form-group">
                                <label for="form-select">Delegación o Municipio*</label>
                                <input type="text" name="Delegation" id="emunicipio">
                            </div>
                            <div class="form-group">
                                <label for="form-select">Colonia*</label>
                                <input type="text" name="Neighborhood" id="ecolonia">
                            </div>
                            <div class="form-group">
                                <label for="send-form-email">Ciudad*</label>
                                <input type="text" name="City" id="eciudad">
                            </div>
                            <div class="form-group">
                                <label for="send-form-name">Estado*</label>
                                <input type="text" name="State" id="eestado">
                            </div>
                            <div class="form-group">
                                <label for="send-form-email">País*</label>
                                <input type="text" name="Country" id="epais">
                            </div>
                            <div class="form-group">
                                <label for="send-form-email">E-mail*</label>
                                <input type="email" name="Billing_email" id="eemail">
                            </div>
                        </div>
                        <div class="col-sm-12 mt10">
                            <input type="submit" data-verif="2" class="btn btn-primary btn-wide pull-right guardar-facturacion" value="Guardar">
                        </div>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>
<!-- /.modal -->

<script type="text/javascript">
function goto_rndf(reload)
   {
    window.location.hash = '#registrar_nuevos_datos_facturacion';
    window.location.reload(true);
   }
</script>

@section scripts {
<script>
    function generate_token() {
        if ($('.form-card').valid()) {
            OpenPay.setId('miqwkvkewii1gqs4ifh2');
            OpenPay.setApiKey('pk_e70b7a22c6c04239b10b7be3596a1f98');
            OpenPay.setSandboxMode(true);

            var deviceSessionId = OpenPay.deviceData.setup("payment-form", "deviceIdHiddenFieldName");

            var sucess_callbak = function (response) {
                var token_id = response.data.id;
            
                    $.ajax({
                        type: 'POST',
                        url: '@Url.Action("RegisterCardAjax", "Payment")',
                        data: { deviceId: deviceSessionId, state: "inactive", token: token_id },
                        success: function (result) {

                            if (result.success == true) {
                                $('#card-number').val("")
                                $('#card-holder-name').val("")
                                $('#card-exp-year').val("")
                                $('#card-exp-month').val("")
                                $('#card-cvv').val("")

                                $('#newCardResult').prepend(
                                    '<div class="alert alert-success alert-dismissible fade in" id="RegisterCardSuccess"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-check"></i>Tarjeta registrada</p></div>');
                                window.setTimeout(function ()
                                { $("#RegisterCardSuccess").alert('close'); }, 10000);
                                window.setTimeout(function () {
                                    $.ajax({
                                        type: 'POST',
                                        url: '@Url.Action("GetCardsAjax", "Payment")',
                                        data: { deviceId: deviceSessionId, state: "inactive", token: token_id },
                                        success: function (result) {
                                            if (result.success == false) {
                                                $('#newCardResult').prepend(
                                                '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                                window.setTimeout(function ()
                                                { $("#GetCardsError").alert('close'); }, 10000);

                                            }
                                            else {
                                                $('#CardList').html(result);
                                            }
                                        },
                                        error: function (emp) {
                                            $('#newCardResult').prepend(
                                                '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                            window.setTimeout(function ()
                                            { $("#GetCardsError").alert('close'); }, 10000);

                                        }
                                    });
                                }, 1000);
                            }
                            else {
                                $('#newCardResult').prepend(
                                '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                window.setTimeout(function ()
                                { $("#RegisterCardError").alert('close'); }, 10000);
                            }
                        },
                        error: function (emp) {
                            $('#newCardResult').prepend(
                                '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al intentar registrar la tarjeta en Antbox.</p></div>');
                            window.setTimeout(function ()
                            { $("#RegisterCardError").alert('close'); }, 10000);

                        }
                    });
            }

            var error_callbak = function (response) {
                var desc = "ERROR [" + response.status + "] " + response.data.description != undefined ? response.data.description : response.message;
                //alert("ERROR [" + response.status + "] " + desc);
                //$("#pay-button").prop("disabled", false);
                switch (response.status) {
                    case 2001:
                        desc = "La cuenta de banco con esta CLABE ya se encuentra registrada en el cliente.";
                        break;
                    case 2002:
                        desc = "La tarjeta con este número ya se encuentra registrada en el cliente.";
                        break;
                    case 2003:
                        desc = "El cliente con este identificador externo (External ID) ya existe.";
                        break;
                    case 2004:
                        desc = "El dígito verificador del número de tarjeta es inválido de acuerdo al algoritmo Luhn.";
                        break;
                    case 2005:
                        desc = "La fecha de expiración de la tarjeta es anterior a la fecha actual.";
                        break;
                    case 2006:
                        desc = "El código de seguridad de la tarjeta (CVV2) no fue proporcionado.";
                        break;
                    case 2007:
                        desc = "El número de tarjeta es de prueba, solamente puede usarse en Sandbox.";
                        break;
                    case 2008:
                        desc = "La tarjeta no es valida para puntos Santander.";
                        break;
                    case 3001:
                        desc = "La tarjeta fue declinada.";
                        break;
                    case 3002:
                        desc = "La tarjeta ha expirado.";
                        break;
                    case 3003:
                        desc = "La tarjeta no tiene fondos suficientes.";
                        break;
                    case 3004:
                        desc = "La tarjeta ha sido identificada como una tarjeta robada.";
                        break;
                    case 3005:
                        desc = "La tarjeta ha sido rechazada por el sistema antifraudes.";
                        break;
                    case 3006:
                        desc = "La operación no esta permitida para este cliente o esta transacción.";
                        break;
                    case 3007:
                        desc = "Deprecado. La tarjeta fue declinada.";
                        break;
                    case 3008:
                        desc = "La tarjeta no es soportada en transacciones en línea.";
                        break;
                    case 3009:
                        desc = "La tarjeta fue reportada como perdida.";
                        break;
                    case 3010:
                        desc = "El banco ha restringido la tarjeta.";
                        break;
                    case 3011:
                        desc = "El banco ha solicitado que la tarjeta sea retenida. Contacte al banco.";
                        break;
                    case 3012:
                        desc = "Se requiere solicitar al banco autorización para realizar este pago.";
                        break;
                }

                $('#newCardResult').prepend(
                    '<div class="alert alert-error alert-dismissible fade in" id="LoginFailAlert"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + desc + '</p></div>');
                window.setTimeout(function ()
                { $("#LoginFailAlert").alert('close'); }, 100000);
            };

            OpenPay.token.create({
                "card_number": $('#card-number').val(),
                "holder_name": $('#card-holder-name').val(),
                "expiration_year": $('#card-exp-year').val(),
                "expiration_month": $('#card-exp-month').val(),
                "cvv2": $('#card-cvv').val()
            }, sucess_callbak, error_callbak);
            //$('#token_id').val(token_id);
            //$('#deviceSessionId').val(deviceSessionId);
        };


    };

    function MakeMainCardAjax(cardId) {

        $.ajax({
            type: 'POST',
            url: '@Url.Action("UpdateCardAjax", "Payment")',
            data: { id: cardId, status: "main" },
            success: function (result) {
                if (result.success == true) {
                    $('#editCardResult').prepend(
                        '<div class="alert alert-success alert-dismissible fade in" id="RegisterCardSuccess"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-check"></i>Tarjeta actualizada</p></div>');
                    window.setTimeout(function ()
                    { $("#RegisterCardSuccess").alert('close'); }, 10000);
                    window.setTimeout(function () {
                        $.ajax({
                            type: 'POST',
                            url: '@Url.Action("GetCardsAjax", "Payment")',
                            data: {},
                            success: function (result) {
                                if (result.success == false) {
                                    $('#editCardResult').prepend(
                                    '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                    window.setTimeout(function ()
                                    { $("#GetCardsError").alert('close'); }, 10000);

                                }
                                else {
                                    $('#CardList').html(result);
                                }
                            },
                            error: function (emp) {
                                $('#editCardResult').prepend(
                                    '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                window.setTimeout(function ()
                                { $("#GetCardsError").alert('close'); }, 10000);

                            }
                        });
                    }, 1000);
                }
                else {
                    $('#editCardResult').prepend(
                    '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al actualizar la tarjeta.</p></div>');
                    window.setTimeout(function ()
                    { $("#RegisterCardError").alert('close'); }, 10000);
                }
            },
            error: function (emp) {
                $('#editCardResult').prepend(
                    '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al intentar actualizar la tarjeta en Antbox.</p></div>');
                window.setTimeout(function ()
                { $("#RegisterCardError").alert('close'); }, 10000);

            }
        });
    }
    function DeleteCardAjax(cardId) {

        $.ajax({
            type: 'POST',
            url: '@Url.Action("DeleteCardAjax", "Payment")',
            data: { id: cardId },
            success: function (result) {
                if (result.success == true) {
                    $('#editCardResult').prepend(
                        '<div class="alert alert-success alert-dismissible fade in" id="RegisterCardSuccess"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-check"></i>Tarjeta eliminada</p></div>');
                    window.setTimeout(function ()
                    { $("#RegisterCardSuccess").alert('close'); }, 10000);

                    window.setTimeout(function () {
                        $.ajax({
                            type: 'POST',
                            url: '@Url.Action("GetCardsAjax", "Payment")',
                            data: {},
                            success: function (result) {
                                if (result.success == false) {
                                    $('#editCardResult').prepend(
                                    '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                    window.setTimeout(function ()
                                    { $("#GetCardsError").alert('close'); }, 10000);

                                }
                                else {
                                    $('#CardList').html(result);
                                }
                            },
                            error: function (emp) {
                                $('#editCardResult').prepend(
                                    '<div class="alert alert-error alert-dismissible fade in" id="GetCardsError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al traer los datos de las tarjetas, intente refrescar la pagina.</p></div>');
                                window.setTimeout(function ()
                                { $("#GetCardsError").alert('close'); }, 10000);

                            }
                        });
                    }, 1000);

                }
                else {
                    $('#editCardResult').prepend(
                    '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + 'ERROR al eliminar la tarjeta.</p></div>');
                    window.setTimeout(function ()
                    { $("#RegisterCardError").alert('close'); }, 10000);
                }
            },
            error: function (emp) {
                $('#editCardResult').prepend(
                    '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + '[ERROR ' + response.status + '] al intentar registrar la tarjeta en Antbox.</p></div>');
                window.setTimeout(function ()
                { $("#RegisterCardError").alert('close'); }, 10000);

            }
        });
    };
    var verif = 1;
    var element = {};
    var formelementg = "";
    var loaderg = "";
    var aliasg = "";
    var verifm = true;
    $(function () {
        $(document).on("click", ".guardar-facturacion", function (event) {
            event.preventDefault();
            verif = $(this).attr("data-verif");

            if (verif == "1") {
                element = $(this);
                formelementg = ".form-afacturacion";
                loaderg = "#loader1";
                var formData = $(".form-afacturacion").serialize();
                
                if ($('.form-afacturacion').valid()) {
                    verifm = true;
                    http('@Url.Action("CreateBillingAddress", "BillingAddress")', formData, callbackSuccess, ".form-afacturacion", loaderg);
                }
            } else {
                formelementg = ".form-efacturacion";
                var formData = $(".form-efacturacion").serialize();
                loaderg = "#loader2";
                verifm = true;
                if ($('.form-efacturacion').valid()) {
                    http('@Url.Action("UpdateBillingAddress", "BillingAddress")', formData, callbackSuccess, ".form-efacturacion", loaderg);
                }
            }
        });
        $(document).on("click", ".open-editar-facturacion", function () {
            element = $(this);
            aliasg = element.parent().find(".identificaciont").text();
            var id = $(this).parent().attr("data-id");
            var formData = {};
            formData.id = id;
            $("#idFacturacion").val(id);
            verifm = false;
            http('@Url.Action("GetBillingAddress", "BillingAddress")', formData, callbackSuccess, ".form-efacturacion", "");
        });
        $(document).on("click", ".eliminar-facturacion", function () {
            element = $(this);
            aliasg = element.parent().find(".identificaciont").text();
            var id = $(this).parent().attr("data-id");
            var formData = {};
            formData.id = id;
            verif = 3;
            verifm = true;
            if (confirm("Esta seguro que desea eliminar la direccion de facturacion " + aliasg + "?")) {
                http('@Url.Action("DeleteBillingAddress", "BillingAddress")', formData, callbackSuccess, ".form-efacturacion", "");
            }
        });

        $('.form-card').validate({
            rules: {
                name: {
                    required: true
                },
                numero: {
                    required: true
                },
                mm: {
                    required: true
                },
                yy: {
                    required: true
                },
                cvv: {
                    required: true
                }
            },
            highlight: function (element) {
                $(element).closest('.form-group').addClass('has-error');
            },
            unhighlight: function (element) {
                $(element).closest('.form-group').removeClass('has-error');
            },
            errorElement: 'span',
            errorClass: 'help-block',
            errorPlacement: function (error, element) {
                if (element.parent('.input-group').length) {
                    error.insertAfter(element.parent());
                } else {
                    error.insertAfter(element);
                }
            }
        });

        $('.form-afacturacion').validate({
            rules: {
                Alias: {
                    required: true
                },
                "Bussiness_name": {
                    required: true
                },
                Street: {
                    required: true
                },
                "External_number": {
                    required: true
                },
                "Rfc_id": {
                    required: true
                },
                Zipcode: {
                    required: true
                },
                Delegation: {
                    required: true
                },
                Neighborhood: {
                    required: true
                },
                City: {
                    required: true
                },
                State: {
                    required: true
                },
                Country: {
                    required: true
                },
                "Billing_email": {
                    required: true
                },
            },
            highlight: function (element) {
                $(element).closest('.form-group').addClass('has-error');
            },
            unhighlight: function (element) {
                $(element).closest('.form-group').removeClass('has-error');
            },
            errorElement: 'span',
            errorClass: 'help-block',
            errorPlacement: function (error, element) {
                if (element.parent('.input-group').length) {
                    error.insertAfter(element.parent());
                } else {
                    error.insertAfter(element);
                }
            }
        });

        $('.form-efacturacion').validate({
            rules: {
                Alias: {
                    required: true
                },
                "Bussiness_name": {
                    required: true
                },
                Street: {
                    required: true
                },
                "External_number": {
                    required: true
                },
                "Rfc_id": {
                    required: true
                },
                Zipcode: {
                    required: true
                },
                Delegation: {
                    required: true
                },
                Neighborhood: {
                    required: true
                },
                City: {
                    required: true
                },
                State: {
                    required: true
                },
                Country: {
                    required: true
                },
                "Billing_email": {
                    required: true
                },
            },
            highlight: function (element) {
                $(element).closest('.form-group').addClass('has-error');
            },
            unhighlight: function (element) {
                $(element).closest('.form-group').removeClass('has-error');
            },
            errorElement: 'span',
            errorClass: 'help-block',
            errorPlacement: function (error, element) {
                colocaChosen();
                if (element.parent('.input-group').length) {
                    error.insertAfter(element.parent());
                } else {
                    error.insertAfter(element);
                }
            }
        });

        var elementButton = null;

        $(".facturar_button").click(function () {
            elementButton = $(this);
            var id = $(this).attr("data-id");
            $("#idpago").val(id);
            $("#facturar").modal("show");
        });

        $('.facturar-button-form').click(function (event) {
            
            event.preventDefault();
            formelementg = ".form-facturar";
            formdata = $(formelementg).serialize();

            var enlace = '@Url.Action("CreateBilling", "Billing")';
            if ($(this).attr("data-billed") == "true") {
                enlace = '@Url.Action("UpdateBilling", "Billing")';
            }

            console.log(enlace);

            $("#loader-facturacion").show();

            $.ajax({
                type: 'POST',
                url: enlace,
                data: formdata,
                success: function (data) {
                    if (data.success)
                    {
                        if (!elementButton.parent().find(".sticker_pay").length)
                        {
                            elementButton.parent().find("h4").after('<div class="sticker_pay completed">Facturado</div>');
                        }
                        $("#facturar").modal("hide");
                    }
                    $('#loader-facturacion').hide();
                },
                error: function (error) {
                    console.log(error);
                    $('#loader-facturacion').hide();
                }
            })
        });
    });
    function callbackSuccess(data)
    {
        if (verifm) {
            if (data.success) {
                if (verif == 1) {
                    cadhtml = htmlFacturacion($("#aidentificacion").val(), $("#arazonsocial").val(), $("#arfc").val(), data.objectaddress.Id);
                    $(".content-datos-facturacion").append(cadhtml);
                    $("#dir-facturacion").append("<option value='" + data.objectaddress.Id + "'>" + $("#aidentificacion").val() + "</option>");
                    $("#dir-facturacion").trigger("chosen:updated");
                } else if (verif == 2) {
                    element.parent().find(".identificaciont").text($("#eidentificacion").val());
                    element.parent().find(".nombreempresat").text($("#erazonsocial").val());
                    element.parent().find(".razonsocialt").text($("#erfc").val());
                    $("#editar_facturacion").modal("hide");
                } else if (verif == 3)
                {
                    element.closest(".div-facturacion").remove();
                }
                if (verif != 3) limpiarCampos(formelementg, loaderg);
            } else {
                var mensaje = (verif == 1) ? "Error al agregar datos de facturacion" : "Error al editar datos de facturacion";
                var mensaje = (verif == 3) ? "Error al eliminar datos de facturacion" : mensaje;
                mostrarError(formelementg, mensaje);
            }
        } else {
            if (data)
            {
                $("#eidentificacion").val(aliasg);
                $("#erazonsocial").val(data.Bussiness_name);
                $("#erfc").val(data.Rfc_id);
                $("#ecalle").val(data.Street);
                $("#eext").val(data.External_number);
                $("#eint").val(data.Internal_number);
                $("#ezipcode").val(data.Zipcode);
                $("#emunicipio").val(data.Delegation);
                $("#ecolonia").val(data.Neighborhood);
                $("#eciudad").val(data.City);
                $("#eestado").val(data.State);
                $("#epais").val(data.Country);
                $("#eemail").val(data.Billing_email);
                $("#editar_facturacion").modal("show");
            }
        }
    }
    function mostrarError(formelement, mensaje)
    {
        $(formelement).prepend(
            '<div class="alert alert-error alert-dismissible fade in" id="RegisterCardError"> <button type="button" class="close" data-dismiss="alert"> <i class="fa fa-times"></i></button><p><i class="fa fa-exclamation-triangle"></i>' + mensaje + '</p></div>');
        window.setTimeout(function ()
        { $("#RegisterCardError").alert('close'); }, 10000);
    }
    function htmlFacturacion(identificacion, razonsocial, rfc, id) {
        var strVar = "";
        strVar += "<div class=\"col-sm-12 back_gris pt30 pb20 mb30  div-facturacion\">";
        strVar += "    <div class=\"media-left\">";
        strVar += "        <div class=\"icon icon-color icon-size-4\">";
        strVar += "            <i class=\"fa fa-file-text\"><\/i>";
        strVar += "        <\/div>";
        strVar += "    <\/div>";
        strVar += "    <div class=\"media-body\" data-id=\"" + id + "\">";
        strVar += "        <h4 class=\"identificaciont\">" + identificacion + "<\/h4>";
        strVar += "        <p>Nombre o Razón Social: <span class=\"nombreempresat\">" + razonsocial + "</span><br>RFC: <span class=\"razonsocialt\">" + rfc + "</span><\/p>";
        strVar += "        <button type=\"button\" class=\"btn btn-primary btn-sm pull-right ml10 open-editar-facturacion\" >Editar<\/button>";
        strVar += "        <button type=\"button\" class=\"btn btn-primary btn-sm pull-right btn_blue eliminar-facturacion\" >Eliminar<\/button>";
        strVar += "    <\/div>";
        strVar += "<\/div>";
        return strVar;
    }
    function limpiarCampos(formelement, loader) {
        $(loader).hide();
        $(formelement).find('input, textarea').each(function () {
            $(this).val("");
        });
    }
    function http(url, formdata, callbackSuccess, formelement, loader)
    {
        if (verifm) $(loader).show();
        $.ajax({
            type: 'POST',
            url: url,
            data: formdata,
            success: callbackSuccess,
            error: function (error) {
                console.log(error);
            }
        })
    }

</script>
    }